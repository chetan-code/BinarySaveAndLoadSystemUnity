# BinarySaveAndLoadSystemUnity

<b>Save and load system which uses binary files to save shape,position and rotation of a Gameobject.</b>

The repo uses binary files to save/load gameobjects form a file.
